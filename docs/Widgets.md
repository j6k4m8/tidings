# Widgets

This is the catalog of reusable UI widgets in Tidings.

## Accent

-   `AccentSwatch`: Small labeled color swatch with selection ring.
-   `AccentSwitch`: Accent-colored toggle switch.

## Forms

-   `LabeledField`: Text field with a label and hint, used in onboarding.
-   `ToggleRow`: Label + `AccentSwitch` row for simple boolean toggles.
-   `AuthFields`: Wrapper that disables and dims auth inputs.

## Settings

-   `SettingsTabBar`: Glassy tab container for settings sections.
-   `SettingsTabLabel`: Standardized tab label padding + text style.
-   `SettingsTab`: Scroll wrapper for each settings tab content.
-   `SettingRow`: Title/subtitle + trailing control layout helper.
-   `SettingsSubheader`: Secondary section header text.
-   `CornerRadiusOption`: Rounded option tile for radius selection.

## Glass

-   `GlassPanel` (theme): Core glass container with variants and accent support.
-   `GlassActionButton`: Accent-tinted call-to-action button.
-   `GlassBottomNav`: Bottom navigation bar with glass styling.

## Background

-   `TidingsBackground`: App-level ambient gradient + glow background.
